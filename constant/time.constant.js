// on miliseconds
const DEFAULT_VERY_SHORT_WAIT = 2000;
const DEFAULT_SHORT_WAIT = 4000;
const DEFAULT_WAIT = 8000;
const DEFAULT_LONG_WAIT = 15000;
const DEFAULT_VERY_LONG_WAIT = 25000;
const DEFAULT_SUPER_LONG_WAIT = 2 * DEFAULT_VERY_LONG_WAIT;

module.exports = {
  DEFAULT_VERY_SHORT_WAIT,
  DEFAULT_SHORT_WAIT,
  DEFAULT_WAIT,
  DEFAULT_LONG_WAIT,
  DEFAULT_VERY_LONG_WAIT,
  DEFAULT_SUPER_LONG_WAIT,
};
